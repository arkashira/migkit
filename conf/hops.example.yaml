hops:
  my-first-hop:
    engine: postgres            # postgres | mysql | mssql | mongodb | redis | kafka
    service: native             # playbook set: aws-dms | tencent-dts | gcp-dms | native
    source:
      host: source.example.com
      port: 5432
      user: postgres
      # secrets need not be plaintext - use a reference resolved at load:
      #   env:NAME / ${NAME}   file:/run/secrets/pw   vault:secret/db#password
      password: env:MIGKIT_SRC_PASSWORD
    target:
      host: 10.0.0.1
      port: 5432
      user: root
      password: secret
    databases: []               # source db names, empty = discover from source
    db_map: {}                  # optional src->dst rename, e.g. {cart_uat: cart}
                                #   config + reports stay keyed by the source
                                #   name; only the target connection is remapped
    workers: 4
    big_rows: 5000000
    slice: 1000000
    # options:
    #   mask: [users.email, ssn]  # values reports show from rows, as salted hashes; `all` masks every value
    #   transfer_price_per_gb: 0.09   # the plan then says what carrying the rows across costs
