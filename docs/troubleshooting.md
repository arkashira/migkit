# Troubleshooting: every refusal, where it comes from

Generated by `python -m migkit.troubleshooting` from the code; do not edit.
Each entry is the message as migkit prints it, with the parts filled in at run time shown as `<...>`, under the part of migkit that raises it. The message says what to do.

## `migkit/assist.py`

* MIGKIT_AI=<...> is not one of <...>

* MIGKIT_AI=<...> needs MIGKIT_AI_MODEL: migkit does not choose a model for this provider

## `migkit/cli.py`

* share_tables needs the s3 state backend (options.state.backend: s3): the machines sharing a move find each other's tables there

* <...>: <...> not configured yet fill host/user/password in conf/hops.yaml, then: migkit doctor

* <...> already exists (use --force to overwrite)

* no playbook for service '<...>', have: <...>

* --convert is for hetero hops, same-engine hops use migkit schema HOP

* <...> could not be created on the target: <...>

* --migration supports postgres and mysql hops

* generating migration DDL needs a component that is not installed on this machine: migkit doctor --install puts it in place

* --drill needs --db and --table (schema.table)

* --drill supports postgres and mysql hops

* <...> has no primary key for joining

* the row-by-row drilldown needs a component that is not installed here: migkit doctor --install puts it in place

* run migkit check first, then sync uses its diffs

* <...>: <...> is writing to this target, and a row repair beside it would race it - the row that survives is whichever lands last. Nothing was applied. Stop it, repair, and start it again, or let it catch up and check again.

* <...>: <...> did not pause within <...>s (hop option repair_window), so nothing was applied. It has been told to carry on.

* <...>: <...>

* <...>: the source's schema changed while it was being moved - <...> ... | <...>. The rows moved before and after it may not line up. Bring the target's schema level with the source's, then move again and run migkit check.

* reverse: <...> - the one there is, is at_cutover

* topology: <...> - one_way or two_way

* topology: two_way - <...>. Nothing was set up.

* the hop maps columns (mapping.columns), and the server's own replication carries whole rows under the source's names. Nothing was set up. `migkit move --mode cdc` follows this hop with migkit's own change tail, which applies the mapping.

* <...>. Nothing was set up. `migkit move --mode cdc` follows this hop with migkit's own change tail, which stops on a schema change instead of carrying half of it. | `migkit move --mode full` copies what the hop covers, and `migkit sync` brings the target level again before cutover.

* cdc tail needs --db

* the streaming pipeline did not come up (logs: docker compose -f <...>/docker-compose.yml logs)

* the copy stopped: <...> <...> | <...>Nothing past the last finished table is marked as moved. The commands that ran are in the run's commands.log.

* protect_target is set, and keeping the application from writing to the target is not available for <...> hops yet - nothing has been copied. Remove the option to move without it.

* moving and then following a <...> source is not set up on this machine; migkit doctor says what is missing

* no way to follow a <...> source is set up on this machine; migkit doctor says what is missing

* the bulk copy reported success and <...> is still empty on the target: <...> ... | <...>. Nothing has been marked as moved - look at the copy's output above, and at the target server's log

* which hop? reports exist for: <...>

* no saved states, run migkit sync --go first

* no state matching '<...>', have: <...>

* state <...> not retrievable from <...>

* replicate not available for <...>, use migkit move --mode cdc or the advise playbook

* this hop follows its source through migkit move --mode cdc

* the move's checkpoint kept changing under migkit while it saved it

## `migkit/config.py`

* pip install boto3 for AWS sign-in and secrets

* secret env var '<...>' is not set

* secret file '<...>' not found

* AWS secret '<...>' holds no text

* could not read AWS secret '<...>': <...>

* AWS secret '<...>' has no field '<...>'

* pip install google-cloud-secret-manager for gcp-sm: secrets

* pip install azure-identity azure-keyvault-secrets for azure-kv: secrets

* could not read vault secret '<...>' (need vault CLI + VAULT_ADDR/VAULT_TOKEN)

* auth: <...> - password (the default) or aws_iam

* no hop configuration yet (<...> does not exist). create one: migkit init or point at an existing file: MIGKIT_CONF=/path/to/hops.yaml

* unknown hop <...>, have: <...>

## `migkit/engines/__init__.py`

* unsupported engine <...>

## `migkit/engines/ase.py`

* FreeTDS's ODBC driver is not on this machine, and SAP ASE is reached through it: brew install freetds, or apt-get install tdsodbc - or say where it is with odbc_driver in the endpoint's options

* ODBC is not usable on this machine (<...>): brew install unixodbc freetds, or apt-get install unixodbc tdsodbc

## `migkit/engines/base.py`

* <...> has no key among the compared columns, so a row cannot be addressed on both sides

* <...> rows of <...> have NULL in the key (<...> ... | <...>) - a row with no key cannot be found on the other side, so repairing it would report success and change nothing. Give those rows a key, or key the hop on a column that is always filled

* <...> cannot be repaired row by row: <...> could not be rendered, and a row written without those columns would be a row the source does not have. Nothing has been written. `migkit move` can re-copy the table, which carries every column whether migkit can compare it or not

* migkit does not <...> on a source

## `migkit/engines/cassandra.py`

* the keyspace <...> is not on the target, and how many copies it keeps is not migkit's to choose: create it, or give the target endpoint `replication`

* <...> moves under a row filter, which is SQL, and Cassandra takes none

* <...>: a row filter is SQL, and Cassandra takes none

* <...> has no key, and a Cassandra table is found by its key: give the table one, or leave it out of this hop

* <...> already exists on the target - migkit will not alter or replace a table that is already there

* <...>: the target refused <...> rows: <...>

## `migkit/engines/clickhouse.py`

* <...> already exists on the target - migkit will not alter or replace a table that is already there

## `migkit/engines/db2.py`

* the <...> needs `database`, the Db2 database to connect to; the hop's databases are its schemas

## `migkit/engines/dbapi.py`

* <...> already exists on the target - migkit will not alter or replace a table that is already there

## `migkit/engines/dynamodb.py`

* <...> moves under a row filter, which is SQL, and DynamoDB takes none

* <...>: a row filter is SQL, and DynamoDB takes none

* a <...> column (<...>) has no DynamoDB counterpart migkit writes; leave it out with mapping.columns

* <...> is keyed by <...> columns, and a DynamoDB table is keyed by one, or by one and a sort key: give the table such a key, or leave it out of this hop

* <...> already exists on the target - migkit will not alter or replace a table that is already there

* <...> has too many columns to describe in a DynamoDB table's tags

## `migkit/engines/generic.py`

* generic engine needs <...>.url in hops.yaml

* generic engine needs options.tables: [t1, t2]

* every table in options.tables is also in the hop's exclude list, so there is nothing to compare

* this engine needs a comparison component that is not installed on this machine: migkit doctor --install puts it in place, and migkit doctor says what is missing

* cannot read the key of <...>: <...>

* cannot open the <...> url to write: <...>

* cannot read the columns of <...>: <...>

* migkit will not repair by <...>: it is <...>, and a key of that type has no one text form to turn back into a value. Key the hop on a column that is a number, a string or a boolean, or repair those rows with the tool that owns that database

* <...> cannot be repaired by <...>: the column is not on both sides, so a row listed by the check cannot be addressed on the target

* <...> has columns the writer would not quote: <...>. An update naming them would be read by the server as some other column, so migkit will not send it - the rows it had already inserted are listed in the report and the check will name the rest again

* <...>.<...> holds binary, and the writer this engine borrows renders bytes as the characters they happen to spell - measured, b'\x00\x01' goes in as a two-character string. Nothing has been written. Move those rows with the tool that owns the target, or leave <...> out of the hop's column list

* <...>.<...> is <...>, which the borrowed writer will not put in an update. Nothing has been written. `migkit move` can re-copy the table, or repair those rows with the tool that owns the target

* a numeric column holds <...>, which is not a number that can be written literally. Nothing has been written; repair that row with the tool that owns the target

## `migkit/engines/hetero.py`

* <...> cannot be 'hetero'

* hetero <...>-><...>: <...> is still written for mysql->postgres only. `check` works on this pair; this does not.

* this pair cannot repair rows: <...> -> <...> does not implement both halves of the read/write contract, so there is nothing to carry the rows with. `migkit assess` lists what the pair can do

* <...> is no longer on both sides, so the rows the last check listed cannot be placed

* <...> is not on the target and migkit cannot build it: no neutral class for <...>. Create the table yourself and run this again - guessing a column type is how a migration arrives complete and wrong

* <...> moves under a row filter, which is SQL, and <...> takes none. Drop the filter, or exclude the table from this hop

* the row filter on <...> (<...>) has no translation into <...>'s SQL (<...>), so the target cannot be read through it. Write it so both engines accept it

* <...>-><...>: one of these engines has no type mapping in migkit, so there is no honest DDL to write for it

* <...> keeps no restore point migkit can take before a repair; use migkit sync --apply, which repairs without one

* <...>: <...> appear under the same name more than once, so migkit cannot tell which target table each goes to. Rename them in the hop's mapping, or exclude the ones not moving.

* <...> has no change log migkit can read, so there is nothing to tail. Re-run `migkit move` for a fresh full load instead, and verify it with `migkit check`

* <...> cannot apply changes yet - it has no statement for writing one row by its key

* the saved position in <...> cannot be read, and a tail started from anywhere else either skips changes or cannot say it did not. Remove the file and move again with --mode full+cdc

* <...>: mapping.columns leaves out part of its key, so a change cannot be placed on the target by it. Keep the key's columns.

* <...>: the source's schema changed under the tail - <...> ... | <...>. The target does not have <...> yet, so nothing after <...> was applied. Bring the target's schema level with the source's, then run the tail again; it resumes from there.

## `migkit/engines/kafka.py`

* security_protocol: <...> is not one of <...>

* sasl_mechanism: <...> is not one of <...>

* the <...> cluster refused the sign-in: <...>. Check the endpoint's user and password, and its sasl_mechanism

* pip install 'migkit[kafka]' for kafka support

* <...> has fewer partitions on the target than on the source: a key's messages would land in another partition, out of the order its consumers read them

* the saved positions in <...> cannot be read, and a tail started from anywhere else sends messages twice or skips them. Remove the file to copy every topic from its start again

* the target refused offsets for <...>: <...>. Offsets committed before this one stand; nothing was deleted

## `migkit/engines/kinesis.py`

* no stream <...> on the target, and a stream costs money for as long as it exists: create it, or say create_streams: true in the hop's options

## `migkit/engines/mongodb.py`

* pip install 'migkit[mongo]' for mongodb support

* a row filter is SQL, and MongoDB takes none; narrow this collection another way

* <...>: migkit will not write documents without _id - there would be no way to write the same row twice without duplicating it

* the server opened a change stream without saying where it stands, so there is no position to start the tail from before the copy

* this server is a standalone, and a change stream needs a replica set - the oplog a stream reads does not exist otherwise

* this server is a standalone, and a change stream needs a replica set - the oplog a stream reads does not exist otherwise. A single node is enough: mongod --replSet rs0 mongosh --eval 'rs.initiate()'

* the saved position in <...> cannot be read, and a tail started from anywhere else either skips changes or cannot say it did not. Remove the file and move again

## `migkit/engines/mssql.py`

* the SQL Server client is not installed on this machine: migkit doctor --install

* mssql row repair is manual, see plan notes

* Change Tracking is off for <...>, and it is the change log migkit reads here: alter database ... set change_tracking = on, then alter table ... enable change_tracking for each table

* Change Tracking does not follow <...> and <...> more | <...>, so their changes would be carried by nothing: alter table ... enable change_tracking for each

## `migkit/engines/mysql.py`

* pip install 'migkit[mysql]' for mysql support

* <...> already exists on the target - migkit will not alter or replace a table that is already there

* <...>: the binlog has more columns than information_schema does, which means the table was altered after this event was written - replaying it would put values in the wrong columns

* no primary key on <...> - a change to a keyless table cannot be addressed on the target, and applying it by matching every column would hit every duplicate. Add a key, or exclude the table

* <...> is <...> on this server, and the tail needs FULL: <...>. set global <...> = 'FULL'; -- self-managed <...>=FULL -- parameter group

* the source no longer has the binlog this tail had read up to (<...>, position <...>): <...>. The changes after it went with it, so starting again from now would skip them without a trace. Move again with --mode full+cdc, and keep the binlog longer than the tail may be stopped for (binlog_expire_logs_seconds, or `binlog retention hours` on RDS)

* the binlog is off on this server, so there is no change log to read - turn on log_bin, or move without CDC

* the MySQL client is not installed on this machine: migkit doctor --install

* <...> is not in the form this migkit reads (<...>). Re-run `migkit check --check data` to write it again before syncing

* <...>: newer_wins names <...>, and it is not on both sides, so which row is newer cannot be told. Nothing was repaired on this table.

## `migkit/engines/opensearch.py`

* <...> moves under a row filter, which is SQL, and OpenSearch takes none

* <...> has no key, and a document's id is its row's key: without one a row written again would be a second document. Give the table a key, or leave it out of this hop

* a <...> column (<...>) has no mapping migkit writes; leave it out with mapping.columns

* <...> already exists on the target - migkit will not alter or replace an index that is already there

* <...>: the target refused <...> documents: <...>: <...>

* <...>: a row filter is SQL, and OpenSearch takes none

## `migkit/engines/parquet.py`

* a <...> column has no Parquet counterpart migkit writes; leave the column out with mapping.columns

* the <...> has no url or path for its Parquet files

* <...> moves under a row filter, which is SQL, and Parquet files take none

* <...> already exists on the target - migkit will not alter or replace a table that is already there

* <...> is not described on the target yet

* <...>: a row filter is SQL, and Parquet files take none

## `migkit/engines/postgres.py`

* <...> already exists on the target - migkit will not alter or replace a table that is already there

* the slot <...> is gone from the source, and the tail had read up to <...> from it. The changes after that were kept by the slot and nowhere else, so starting again from now would skip them without a trace. Move again with --mode full+cdc, which makes the slot before it copies

* the slot <...> was made with the <...> plugin and migkit reads <...>. Reading one plugin's output as another's does not fail, it mis-parses - drop the slot and let migkit make it, or point this hop at another name

* wal_level is <...> on this server and a logical slot needs 'logical'. Nothing migkit does client-side can make the WAL carry row images it was not told to carry. alter system set wal_level = 'logical'; -- then restart rds.logical_replication = 1 -- parameter group, then reboot

* no primary key on <...> - a change to a keyless table cannot be addressed on the target, and applying it by matching every column would hit every duplicate. The change is still in the slot and every call will stop here again, because peeking never throws anything away. Give the table a key or a unique REPLICA IDENTITY, or step past it with: select pg_replication_slot_advance('<...>', pg_current_wal_lsn()); -- skips everything pending, including this

* the <...>: <...>. The connection was refused before it ran anything.

* <...>: newer_wins names <...>, and it is not on both sides, so which row is newer cannot be told. Nothing was repaired on this table.

* MIGKIT_SUBSCRIBE_TIMEOUT=<...> is not a whole number of seconds greater than zero

## `migkit/engines/pubsub.py`

* the target names no project: say project: in its options

* topic <...> has no subscription, and Pub/Sub keeps a message only for the subscriptions a topic has when it is published - these changes would be dropped. Create the subscription that will read them first

* no topic <...> in project <...>: create it, and the subscription that will read it

* Pub/Sub did not take a change for topic <...>: <...>. Started again, the tail goes on from the position it last saved

## `migkit/engines/redis.py`

* pip install 'migkit[redis]' for redis support

* <...>: the target refused a key it was handed: <...>. A payload version error means it runs an older Redis than the source, whose values do not load into an older version

* RESTORE of <...> was refused by the target: <...>. <...> keys were copied before this one and nothing has been deleted. A payload version error means the target runs an older Redis than the source: its payloads do not load into an older version, so these keys need re-writing value by value, which migkit does not do yet (backlog item 0e)

* the <...> refused <...>: <...><...>. What ran before it stays as it is.

## `migkit/engines/sqlite.py`

* <...> already exists on the target - migkit will not alter or replace a table that is already there

* no target database file is configured

* <...>: the directory <...> does not exist, so migkit will not create the target file - make the directory first if this is really where the data should land

* sqlite cannot apply a <...> action

## `migkit/engines/warehouse.py`

* the <...> needs `database`, the database to connect to; the hop's databases are its schemas

* the <...> needs `account`, the Snowflake account identifier

* the Snowflake driver is not installed: pip install snowflake-connector-python

* the BigQuery driver is not installed: pip install google-cloud-bigquery

## `migkit/lease.py`

* MIGKIT_LEASE_SECONDS is not a number of seconds

* the hop's lease kept changing under migkit while it tried to write it; another machine is taking it over and back - stop the other runs

## `migkit/movers.py`

* MIGKIT_MOVER=<...> is not one of <...>

* MIGKIT_MOVER=<...> does not apply to a <...> hop

* the hop maps row filters onto <...>, and nothing on this <...> path can apply one: neither its bulk copy nor its table copier takes a row predicate. Moving anyway would copy every row and leave `check` comparing a filtered source against a full target for good. Drop the filters, or narrow the source with a view the hop points at instead.

* <...> would be emptied anyway: the table references one of the tables this move replaces, and emptying a table empties everything pointing at it. the hop excludes it, which says its rows are written here and not carried, so they cannot be put back afterwards. either it is not target-owned after all - take it out of the hop's exclude list and let the move carry it - or the reference has to go before the move can run.

* <...>: <...> ... | <...> have triggers on the target - foreign keys among them - and <...> can neither turn them off for the load nor load as a replica. Loading anyway would either be refused row by row or let the triggers rewrite what arrives. Nothing has been written. Grant it with `GRANT SET ON PARAMETER session_replication_role TO <...>` (PostgreSQL 15 and later), or move as the managed service's admin user.

* the installed bulk copy program accepts none of <...>, which this move needs - `migkit doctor --install` puts a version in place that does

* MIGKIT_CDC=<...> is not one of native, follow. native = the database's own replication, which needs the target to dial the source; follow = a leg driven from where migkit runs, which connects out to both sides instead

* this CDC path needs a component that is not installed on this machine: it runs for the length of the catch-up and keeps its state in <...>. migkit doctor --install puts it in place, or use MIGKIT_CDC=native, which needs no component here but does need the target to reach the source

* the CDC leg exited <...> before it finished catching up; its log is <...>

* the CDC leg did not reach its end position in <...>s. It is still running; its log is <...>. Nothing has been marked as caught up - raise MIGKIT_FOLLOW_TIMEOUT if the backlog is simply larger than that

* the CDC leg never became ready to be told to apply; its log is <...>

* the CDC leg exited before it was told to apply anything, so nothing reached the target; its log is <...>

* could not set the <...> on the CDC leg:<...>

* no bulk path named <...>

* this bulk path needs programs this machine does not have - migkit doctor --install puts them in place

* this bulk path needs a component that is not available on this machine - migkit doctor says what is missing and migkit doctor --install puts it in place

* the <...> an earlier move of <...> started is still running (process <...>, since <...>): the run that started it was stopped without stopping it, and it is still writing. A move started beside it would empty the tables under it. Wait for it to finish, or stop it (`kill <...>`), then run the move again. Nothing has been written by this run

* the installed <...> does not take options the move passes it: <...>. Nothing has been written. migkit doctor --install puts a build that does in place

* <...>: <...> ... | <...> would fire for every row the load writes, and could rewrite them; <...> cannot create them again as their definer after taking them off. Nothing has been loaded. Load as an account allowed to set a definer (SET_USER_ID), or take them off and put them back yourself.

* <...>: the target's triggers could not be saved before taking them off for the load, so nothing was loaded

* <...>: <...> triggers were not put back: <...>. Their definitions are in <...>

## `migkit/report.py`

* no summary at <...>, run migkit check first

## `migkit/second_reader.py`

* no second reading is set up for <...> hops

## `migkit/state.py`

* unknown state backend '<...>', use local or s3

* this restore point is encrypted: set MIGKIT_STATE_KEY to the passphrase it was written with

* MIGKIT_STATE_KEY is not the passphrase this restore point was written with

* s3 state backend needs 'bucket' in hops.yaml

* pip install boto3 for the s3 state backend

## `migkit/streamout.py`

* format: <...> is not one of <...>

## `migkit/tailctl.py`

* <...>: <...>. Nothing was applied. Move again with --mode full+cdc.

* <...>: the saved change position belongs to another source (<...>); the source is now <...>. A failover or a rebuilt server gives the same address a log of its own, and resuming in it skips what the old one held. Nothing was applied. Move again with --mode full+cdc, or remove <...> and <...> once the target is known to be level with the new source.

## `migkit/users.py`

* users: engine <...> not supported

* setpw: mongodb only (other engines can copy the hash)
